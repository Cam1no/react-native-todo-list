const initialState = {
  uid: '',
  displayName: '',
  email: '',
  password: '',
}

const currentUser = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default currentUser;
